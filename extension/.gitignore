# Environment variables (secrets — only .env.example is committed)
.env
.env.local
.env.*.local

# Extension signing key (pins the extension ID across Chrome/Edge dev builds).
# The matching public key lives in wxt.config.ts (manifest.key) and is safe to
# commit; the private key never is.
*.pem

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
stats.html
stats-*.json
.wxt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
